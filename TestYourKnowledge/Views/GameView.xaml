<Page x:Class="TestYourKnowledge.Views.GameView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestYourKnowledge.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GameView">
    <Grid Background="{StaticResource BackgroundDarkColorBrush}">
        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Margin="20" HorizontalAlignment="Center">
                <TextBlock Style="{StaticResource BaseTextBlock}" Text="{Binding Name}" Width="400"/>
                <TextBlock Text="{Binding TimeLeft}" Style="{StaticResource BaseTextBlock}"/>
            </StackPanel>
            <Button Content="Main Menu" Command="{Binding MainMenuCommand}" Height="30" VerticalAlignment="Bottom"/>

        </StackPanel>

    </Grid>
</Page>
